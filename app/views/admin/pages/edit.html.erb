<h1><%= t("Editing Page") %></h1>
<%= error_messages_for :pae %>
<% form_for(:page, :url => object_url, :html => { :method => :put }) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%=submit_tag t("Update")%>
  </p>
<% end %>