<h1><%= t("New Page") %></h1>

<%= error_messages_for :page %>

<% form_for(:page, :url => collection_url) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= submit_tag t("Create") %>
  </p>
<% end %>